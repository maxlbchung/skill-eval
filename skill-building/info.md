# How it works

Task it with testing a certain skill

extracts testing folder from skill

It creates 6xN agents with different models with and without skill

displays lives subagent token count and step count. also shows time spent

runs tests using info in testing folder. tracks time spent.

stores test results in a db

db can be visualized with the report.html generated with each run