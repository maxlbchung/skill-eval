{
  "name": "skill-eval",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Skill tester v2 — evaluates a skill across models × conditions × replicates, files-as-truth with a SQLite query store.",
  "scripts": {
    "run": "node scripts/run.js",
    "start": "node scripts/server.js"
  },
  "engines": {
    "node": ">=22.5"
  }
}
